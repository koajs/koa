#!/usr/bin/env bash

echo
defaultServerPath="$(dirname "$0")/middleware.js"
MW=${1:-20} node ${2:-$defaultServerPath} &
pid=$!

sleep 2

wrk 'http://localhost:3333/?foo[bar]=baz' \
  -d 3 \
  -c 50 \
  -t 8 \
  | grep 'Requests/sec' \
  | awk '{ print "  " $2 }'

kill $pid
